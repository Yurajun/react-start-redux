extends /blocks/layout-default/layout-default
include /blocks/wrapper-app/wrapper-app

block head
	- var pageTitle = 'CSSSR Project Template';

block content
	+wrapper-app

		+b('input').trackInput(type="text")
		+b('button').addTrack Add Track
		+b('ul').list
